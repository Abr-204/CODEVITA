#include <bits/stdc++.h>
using namespace std;

void constellation(vector<vector<char>>& m,int n){
    for(int i=0;i<n;i++){
        if(m[0][i]=='#' && m[1][i]=='#' && m[2][i]=='#'){
            cout<<"#";
        }
        else if(m[0][i]=='.' && m[1][i]=='.' && m[2][i]=='.'){
            
        }
        else{
            int x1=i;
            char a,b,c,a1,b1,c1,a2,b2,c2;
            a=m[0][x1];
            b=m[0][x1+1];
            c=m[0][x1+2];
            a1=m[1][x1];
            b1=m[1][x1+1];
            c1=m[1][x1+2];
            a2=m[2][x1];
            b2=m[2][x1+1];
            c2=m[2][x1+2];
            
            if(a=='.' && b=='*' && c=='.' && a1=='*' && b1=='*' && c1=='*' && a2=='*' && b2=='.' && c2=='*'){
                cout<<"A";
                i=i+2;
            }
            if (a == '*' && b == '*' && c == '*' && a1 == '*' && b1 == '*' && c1 == '*'
                && a2 == '*' && b2 == '*' && c2 == '*') {
                    cout<<"E";
                    i=i+2;
            }
            if(a=='*' && b=='*' && c=='*' && a1=='.' && b1=='*' && c1=='.' && a2=='*' && b2=='*' && c2=='*'){
                cout<<"I";
                i=i+2;
            }
            if (a == '*' && b == '*' && c == '*' && a1 == '*' && b1 == '.' && c1 == '*' && a2 == '*' && b2 == '*' && c2 == '*') { 
                cout<<"O";
                i=i+2;
            }
            if(a=='*' && b=='.' && c=='*' && a1=='*' && b1=='.' && c1=='*' && a2=='*' && b2=='*' && c2=='*'){
                cout<<"U";
                i=i+2;
            }
        }
    }
}

int main() {
	int n=18;
	vector<vector<char>>m={ { '*', '.', '*', '#', '*', '*', '*', '#', '*', 
              '*', '*', '#', '*', '*', '*', '.', '*', '.' }, 
            { '*', '.', '*', '#', '*', '.', '*', '#', '.', 
              '*', '.', '#', '*', '*', '*', '*', '*', '*' }, 
            { '*', '*', '*', '#', '*', '*', '*', '#', '*', 
              '*', '*', '#', '*', '*', '*', '*', '.', 
              '*' } }; 
              constellation(m,n);

}
